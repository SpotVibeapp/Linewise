name: linewise
description: Linewise — source-backed sports line research, pick generation and calibration.
publish_to: 'none'
version: 1.11.0+12

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: 3.0.5
  flutter_secure_storage: 9.2.2
  http: 1.2.2
  path_provider: 2.1.4
  shared_preferences: 2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
